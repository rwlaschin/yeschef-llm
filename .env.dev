# ============================================================
# Dev only - overrides .env
# ============================================================

GCP_PROJECT_ID=yeschef-c572a
FIREBASE_PROJECT_ID=yeschef-c572a
GOOGLE_APPLICATION_CREDENTIALS=/Users/mac/Documents/Work/alimenta/yeschef-c572a-firebase-adminsdk-fbsvc-f2933f9fbd.json

# MongoDB Atlas
# DB must match where the dashboard writes prompt_library + regulations (RAG): `yeschef`.
# (`yeschef_dev` is empty — the worker found 0 prompts there, so the planner got no system
#  prompt and the model answered the request directly instead of emitting a plan.)
MONGO_URI=mongodb+srv://rwuser:UNDERDOG*dish9consort@yeschef-development.vfmtkz4.mongodb.net/?appName=YesChef-Development
MONGO_DB=yeschef

# Neo4j Aura
NEO4J_URI=neo4j+s://0b4b8b3f.databases.neo4j.io
NEO4J_USERNAME=0b4b8b3f
NEO4J_PASSWORD=4Xdp76QlCd-ebl20jYgZbK1A2Hx-zw_pYbE1VLfWmxQ

OLLAMA_MODEL=llama3.2:2b

# Single-CPU / ~8GB dev box: run ONE generation slot so a request gets the whole CPU.
# parallel=2 split the core and starved units past the 120s stall cap. (Overrides .env=2.)
OLLAMA_NUM_PARALLEL=1
